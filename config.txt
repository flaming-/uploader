##   set your own values below
##   make sure you leave the quotes and stuff

USERNAME = 'USERNAME'    
PASSWORD = 'PASSWORD'
default_folder = '/' 				## root

verbose = True      ## should it print out status and stuff or just be quiet
debug = False       ## displays full traceback instead of short error

flac_path = os.path.join('codecs','flac')   ## can be either relative or not
lame_path = os.path.join('codecs','lame')

main_action = 'perfect_three_and_upload'
